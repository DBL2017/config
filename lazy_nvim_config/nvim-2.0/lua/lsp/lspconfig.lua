-- local self-made LSP plugin (lazy-loaded on buffer open)
return {
    -- dir = "~/projects/nvim-lsp-local",
    -- name = "nvim-lsp-local",
    -- lazy = true,
    -- event = { "BufReadPre", "BufNewFile" },
    -- config = function()
    --     -- require the module to run the LSP setup
    --     require("lspconfig_local")
    -- end,
}
