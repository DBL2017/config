return {
    "SmiteshP/nvim-navic",
    enabled = false,
    dependencies = { "neovim/nvim-lspconfig" },
    -- config = function()
    --     -- require("lspconfig").clangd.setup({
    --     --     on_attach = function(client, bufnr)
    --     --     end,
    --     -- })
    -- end,
}
